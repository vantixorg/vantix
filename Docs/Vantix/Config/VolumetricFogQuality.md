# VolumetricFogQuality

`Vantix.Config.VolumetricFogQuality`

Volumetric fog quality; Low/Medium/High = 64/96/160 voxels/side (cubic cost). Off must not be used at runtime - smoke grenades render into the same fog texture, so Off makes smokes invisible.
