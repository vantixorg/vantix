# SnapshotFlags

`Vantix.Net.SnapshotFlags`

Bit flags packed into `Flags`.

## Fields

| Name | Summary |
|------|---------|
| `WorldReady` | Client finished world preloads (`WorldInitComplete`); cleared on respawn/reconnect. Puppet TPS body stays hidden while unset. |
