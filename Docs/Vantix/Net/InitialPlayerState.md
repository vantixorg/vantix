# InitialPlayerState

`Vantix.Net.InitialPlayerState`

One player's spawn state sent at join, before the first snapshot arrives.

## Fields

| Name | Summary |
|------|---------|
| `Team` | Cast of `Team`; sent at join so puppets show team-glow before the first snapshot. |
| `TeamSlot` | See `TeamSlot`; sent at join for the right colour pre-snapshot. |
