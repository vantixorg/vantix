# PrintInterpolatedStringHandler

`Vantix.Utils.PrintInterpolatedStringHandler`

InterpolatedStringHandler for `Print`. The `out bool shouldAppend` constructor convention lets the compiler skip all Append calls when `Enabled` is false (zero alloc).
