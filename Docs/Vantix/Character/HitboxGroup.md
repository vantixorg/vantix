# HitboxGroup

`Vantix.Character.HitboxGroup`

Hitbox zone enum for damage routing; keys `Damages` and serialised as a byte in HitEvent packets (see `WriteHit`). Append-only — reordering values breaks the wire protocol.
