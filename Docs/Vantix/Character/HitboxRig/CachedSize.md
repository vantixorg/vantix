# CachedSize

`Vantix.Character.HitboxRig.CachedSize`

Cache of computed hitbox sizes per bone index, populated on the first AutoSize run and reused by later spawns. Flat key by bone index assumes a single character mesh.
